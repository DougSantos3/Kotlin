/* Kotlin tem um conceito semelhante ao final do Java, mas usa a palavra-chave open para marcar uma classe ou
método como "aberto" para extensão.

Por padrão, todas as classes e métodos em Kotlin são final — ou seja, não podem ser herdados ou sobrescritos, a menos
que você declare explicitamente que são open. Isso é o oposto do Java, onde as classes e métodos são "abertos" por
padrão. */
