package br.com.vraptor.bitBank.begin

class DataClass {

    /* As Data Classes no Kotlin são muito utilizadas quando queremos declarar classes com o objetivo principal de
    armazenar dados. Uma das vantagens de utilizar data classes é o fato de já trazer alguns métodos implementados que
     são super úteis durante o desenvolvimento, como:

    equals()
    toString() */
}