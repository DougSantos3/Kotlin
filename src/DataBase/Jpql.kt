package br.com.vraptor.bitBank.banco

class Jpql {
    /* A JPQL é a linguagem de consulta padrão da especificação JPA! Já a HQL é a linguagem de consulta do Hibernate!
    Isso quer dizer que o JPQL vai funcionar se vc estiver usando o Hibernate ou outra implementação da especificação
    JPA ( como o EclipseLink, por exemplo ). Já a HQL só funcionará se vc estiver usando o Hibernate! E sim, como a HQL
    é de propriedade do Hibernate, ela terá funcionalidades extras além das definidas pela JPQL! */
}